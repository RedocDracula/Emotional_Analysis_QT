#include <vector>
#include <iostream>
#include <QtCore>


long long int FindLowerBound(vector <long long int> subtitle, qint64 number)
{
            int left = 0, right = subtitle.size();
            int mid;

            while(right>left)
             {
                mid = (left+right)/2;
                if(v[mid]==number)
                {

                }
             }
}
